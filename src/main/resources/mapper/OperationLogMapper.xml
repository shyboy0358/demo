<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OperationLogMapper">

    <resultMap id="operationLogMap" type="com.example.demo.entity.OperationLog">
        <id column="id" property="id"/>
        <result column="operator" property="operator"/>
        <result column="operation_type" property="operationType"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="content" property="content"/>
        <result column="ip" property="ip"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findAll" resultMap="operationLogMap">
        SELECT * FROM operation_log ORDER BY create_time DESC
    </select>

    <select id="findByTargetType" resultMap="operationLogMap">
        SELECT * FROM operation_log WHERE target_type = #{targetType} ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (operator, operation_type, target_type, target_id, content, ip, create_time)
        VALUES (#{operator}, #{operationType}, #{targetType}, #{targetId}, #{content}, #{ip}, NOW())
    </insert>

</mapper>
